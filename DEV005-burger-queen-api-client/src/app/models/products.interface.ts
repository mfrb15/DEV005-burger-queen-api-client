export interface Product {
  id: number,
  name: string,
  price: number,
  image: string,
  type: string,
  category?: string,
  dateEntry: string,
}

export interface tabButton {
  name: string,
  label: string,
}
